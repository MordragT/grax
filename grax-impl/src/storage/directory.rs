// directories correspond to children
// nodes can have value -> file and children -> directory
// both have the same name
// nodes wich are already in the graph and are connected to another node are linked
// otherwise they are saved under their corresponding parent
// identifiers are simple strings
